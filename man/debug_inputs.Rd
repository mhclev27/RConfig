% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/function_troubleshooting.R
\name{debug_inputs}
\alias{debug_inputs}
\title{Bind a function's resolved arguments into the global environment for debugging}
\usage{
debug_inputs(fn, ...)
}
\arguments{
\item{fn}{The function whose call you want to simulate.}

\item{...}{Arguments to \code{fn}, named and/or positional, exactly as you
would call \code{fn} itself.}
}
\value{
Invisibly, the named list of resolved argument values (also
assigned into \code{.GlobalEnv} as a side effect).
}
\description{
Given a function \code{fn} and a set of arguments — named, positional, or a
mix, matched the same way R would match a real call to \code{fn} — evaluates
whatever you explicitly supplied and fills in \code{fn}'s own default values
for anything you didn't, then binds the whole resolved argument set
directly into \code{.GlobalEnv}. Lets you step through \code{fn}'s body line by
line as if you were inside a real call to it, without \code{debug()}/\code{browser()}.
}
\details{
To debug a call like \code{my_func(a = 1, x)}, change it to
\code{debug_inputs(my_func, a = 1, x)} — insert \code{fn} as the first argument
and keep the rest of the call as-is, rather than renaming \code{my_func}
itself (renaming would lose the information needed to look up defaults).

Defaults that reference other parameters are supported (e.g.
\verb{function(a, b = a * 2)} resolves \code{b} correctly using the bound value
of \code{a}). Required arguments (no default) that you don't supply are
simply left unbound, since there's nothing to fill them in with.
}
