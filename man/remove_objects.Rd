% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/memory_functions.R
\name{remove_objects}
\alias{remove_objects}
\title{Remove objects from an environment and optionally run garbage collection}
\usage{
remove_objects(
  objects = NULL,
  keep = NULL,
  envir = .GlobalEnv,
  run_gc = TRUE,
  restart = F,
  verbose = TRUE
)
}
\arguments{
\item{objects}{Character vector of object names to remove. If NULL,
removes everything in \code{envir} except \code{keep}.}

\item{keep}{Character vector of object names to preserve when \code{objects} is NULL.}

\item{envir}{Environment to remove objects from. Defaults to \code{.GlobalEnv}.}

\item{run_gc}{If TRUE (default), runs \code{gc()} after removal.}

\item{restart}{If TRUE, restarts the R session (RStudio only) after cleanup.}

\item{verbose}{If TRUE (default), messages what was removed.}
}
\value{
Invisible \code{NULL}, or the result of \code{gc()} if \code{run_gc = TRUE}.
}
\description{
Removes \code{objects} (or everything except \code{keep}, if \code{objects} is NULL)
from \code{envir}, then optionally runs \code{gc()} and/or restarts the R session.
}
