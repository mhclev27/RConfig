% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotFunctions.R
\name{gsave}
\alias{gsave}
\title{Save a ggplot with a companion PDF, RDS, source data, and README}
\usage{
gsave(
  title,
  plot = NULL,
  dir,
  w,
  h,
  s = 1,
  folders = NULL,
  dfs,
  dpi = 400,
  additional_info = TRUE,
  desc = NULL,
  type = ".jpeg",
  units = "inches"
)
}
\arguments{
\item{title}{Base filename (without extension) and subfolder name.}

\item{plot}{ggplot object to save. Defaults to \code{last_plot()} if NULL.}

\item{dir}{Output directory.}

\item{w, h}{Width/height to save at.}

\item{s}{Scale multiplier applied to \code{w}/\code{h}.}

\item{folders}{Unused (reserved).}

\item{dfs}{Named list of data frames to save alongside the plot as CSVs.}

\item{dpi}{Resolution for raster output.}

\item{additional_info}{Option to save plot object, dfs, description readme. Default is TRUE}

\item{desc}{Optional description saved as \code{readme.txt} alongside the plot.}

\item{type}{File extension for the primary saved plot (default ".jpeg").}

\item{units}{Units for \code{w}/\code{h}: "px"/"pixels", or anything else treated as inches.}
}
\value{
Invisible; called for its side effects (files written to disk).
}
\description{
Saves \code{plot} to \code{dir} as \code{type} (default .jpeg), plus a companion PDF
(unless \code{type} is already .pdf), the plot object as an RDS, any data
frames in \code{dfs} as CSVs, and an optional \code{desc} as a readme.txt — all
inside a subfolder named after \code{title}.
}
